import { describe, it, expect } from "vitest";
import request from "supertest";

const expressAPI = request("http://localhost:3000");

describe("PUT (/publication/like/:id) tests", () => {
  it("", async () => {
    expect(true).toBe(true);
  });
});
